apiVersion: v1
kind: Pod
metadata: 
  name: env-configmap
spec:
  containers:
    - name: naginx
      command: ["/bin/bash", "-c", "printenv"]
      image: busybox:latest
      envFrom:
        - configMapRef:
            name: pod-config
